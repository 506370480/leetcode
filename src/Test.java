import other.MinStack;

import java.util.*;

public class Test {
    public static void main(String[] args) {
        Queue<Map<Integer,Integer>> queue = new LinkedList<>();
        Map<Integer,Integer> map = new HashMap<>();
        map.put(1, 1);
        System.out.println(map.get(2));
        queue.size();
    }
}
